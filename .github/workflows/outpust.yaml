name: Checking Outputs
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step one
        id: abc
        run: echo "version=1.0.1" >> $GITHUB_OUTPUT
      - name: step two
        run: echo "This is from step one ${{ steps.abc.outputs.version  }}"
